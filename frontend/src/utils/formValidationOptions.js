export const nameOptions = {
  required: true,
  minLength: 2,
  maxLength: 30,
};

export const emailOptions = {
  required: true,
};

export const passwordOptions = {
  required: true,
  minLength: 4,
  maxLength: 30,
};
